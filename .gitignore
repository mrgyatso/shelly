# dependencies
node_modules

# build output
dist/
build/
.vite/
ui-dist/
target/
dist-demo/
# generated from index.html by scripts/gen-demo-html.mjs
overlay/demo.html

# Local replay kit — real artifacts from real sessions. This repo is PUBLIC and the
# demo is published; only the invented demo-*.html artifacts may be committed.
demo/replay-prompts.md
demo/artifacts/*
!demo/artifacts/demo-*.html

# runtime state (persisted checkbox state)
state.json

# env / secrets
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

.claude/settings.local.json

# Session handoff notes — working files, not part of the product. Archived to
# ~/shelly-notes/handoffs. This repo is PUBLIC; keep them out.
HANDOFF-*.md
.claude/settings.json
